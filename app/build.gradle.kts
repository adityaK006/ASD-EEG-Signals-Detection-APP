// build.gradle.kts — keep minimal or empty when using version catalogs
// If needed, leave it empty or just include this for clarity
fun plugins() {
// No need to declare AGP or Kotlin here, handled via libs.versions.toml
}